<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Weather Forecast</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <div class="container">
    <h1>Weather Forecast</h1>

    <form action="/weather" method="GET">
      <input type="text" name="city" placeholder="Enter city name" required>
      <input type="date" name="date" required>
      <button type="submit">Get Weather</button>
    </form>

    <% if (weather) { %>
      <div class="weather-info">
        <h2><%= weather.city %>, <%= weather.country %></h2>
        <p><strong><%= weather.date %></strong></p> <!-- Render formatted date -->
        <p><%= weather.description %></p>
        <img src="http://openweathermap.org/img/wn/<%= weather.icon %>.png" alt="weather icon" width="50px" height="50px">
        <p>Temperature: <%= weather.temperature %>°C</p>
        <p>Feels like: <%= weather.feelsLike %>°C</p>
        <p>Humidity: <%= weather.humidity %>%</p>
        <p>Wind Speed: <%= weather.windSpeed %> m/s</p>
        <% if (weather.isCloudy) { %>
          <p><strong>Bring an umbrella!</strong></p>
        <% } else { %>
          <p><strong>No need for umbrella.</strong></p>
        <% } %>
      </div>
      <% } %>
    

    <% if (error) { %>
      <p class="error"><%= error %></p>
    <% } %>
  </div>

  <footer class="footer">
    &copy; 2024 MUHAMMED-ADESINA. All rights reserved.
  </footer>
</body>
</html>
